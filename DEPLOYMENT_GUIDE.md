# دليل نشر منصة YASSER AI

## 📋 المتطلبات

### متطلبات النظام:
- Node.js 18+
- npm أو pnpm
- مفتاح OpenAI API

### متطلبات البيئة:
```bash
OPENAI_API_KEY=your_api_key_here
```

---

## 🚀 خطوات النشر

### 1. التثبيت المحلي

```bash
# استنساخ المشروع
git clone https://github.com/yasser/ai-platform.git
cd ai_platform_yasser

# تثبيت المكتبات
npm install

# إنشاء ملف .env
echo "OPENAI_API_KEY=your_key_here" > .env
```

### 2. التشغيل المحلي

```bash
# تشغيل الخادم والتطبيق معاً
npm run start

# أو تشغيل كل منهما بشكل منفصل:
# الخادم
node server-advanced.js

# التطبيق (في نافذة أخرى)
npm run dev
```

### 3. البناء للإنتاج

```bash
npm run build
```

---

## 🌐 خيارات النشر

### خيار 1: Vercel (موصى به)

```bash
# تثبيت Vercel CLI
npm i -g vercel

# النشر
vercel
```

### خيار 2: Netlify

```bash
# تثبيت Netlify CLI
npm i -g netlify-cli

# النشر
netlify deploy --prod
```

### خيار 3: Heroku

```bash
# تثبيت Heroku CLI
npm i -g heroku

# تسجيل الدخول
heroku login

# إنشاء تطبيق
heroku create your-app-name

# ضبط متغيرات البيئة
heroku config:set OPENAI_API_KEY=your_key_here

# النشر
git push heroku main
```

### خيار 4: Docker

```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

ENV OPENAI_API_KEY=your_key_here

EXPOSE 5000 5173

CMD ["npm", "run", "start"]
```

---

## 🔧 الإعدادات الإنتاجية

### 1. متغيرات البيئة
```
OPENAI_API_KEY=your_production_key
NODE_ENV=production
PORT=5000
```

### 2. أمان الخادم
- تفعيل HTTPS
- تحديد معدلات الطلب
- التحقق من صحة المدخلات
- استخدام CORS بحذر

### 3. الأداء
- تفعيل caching
- ضغط الملفات
- تحسين الصور
- استخدام CDN

---

## 📊 المراقبة

### تتبع الأخطاء:
```bash
# استخدام Sentry
npm install @sentry/node
```

### تحليلات الاستخدام:
```bash
# استخدام Google Analytics
npm install react-ga
```

---

## 🧪 الاختبار

```bash
# اختبار الخادم
curl http://localhost:5000/api/stats

# اختبار التطبيق
curl http://localhost:5173
```

---

## 🐛 استكشاف الأخطاء

### المشكلة: خطأ في الاتصال بـ OpenAI
**الحل**: تحقق من مفتاح API والاتصال بالإنترنت

### المشكلة: الخادم لا يبدأ
**الحل**: تحقق من المنافذ المتاحة وتثبيت المكتبات

### المشكلة: بطء الاستجابة
**الحل**: تحقق من حد معدل الطلب وأداء الخادم

---

## 📞 الدعم

للمساعدة والدعم:
- 📧 البريد: support@yasser-ai.com
- 🐛 المشاكل: GitHub Issues
- 💬 المجتمع: Discord Server

---

**آخر تحديث**: نوفمبر 2025
**الإصدار**: 2.0.0
